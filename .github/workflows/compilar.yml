name: Compilar BespokeSynth para Void Linux (Musl)

on:
  workflow_dispatch:

jobs:
  build-musl:
    runs-on: ubuntu-latest

    container:
      image: ghcr.io/void-linux/void-musl:latest

    steps:
      # 1. Actualizar el sistema e instalar herramientas de Void Linux
      - name: Instalar herramientas esenciales y dependencias de audio
        run: |
          # En Void, a veces es necesario correr la actualización dos veces si el gestor xbps se actualiza a sí mismo
          xbps-install -Syu || xbps-install -Syu
          xbps-install -y git cmake gcc make pkg-config python3 \
            alsa-lib-devel jack-devel libX11-devel libXrandr-devel \
            libXinerama-devel libXcursor-devel freetype-devel libcurl-devel \
            nodejs bash libXext-devel libXrender-devel libXi-devel glib-devel \
            MesaLib-devel gtk+3-devel libwebkit2gtk41-devel libexecinfo-devel

      # 2. Clonar el código fuente oficial de BespokeSynth con sus submódulos
      - name: Clonar código fuente y submódulos
        run: |
          git clone --recursive https://github.com/BespokeSynth/BespokeSynth.git

      # 3. PARCHE CRÍTICO MUSL: Limpiar dependencias incompatibles de glibc
      - name: Parchear JUCE para entorno Musl
        run: |
          cd BespokeSynth/libs/JUCE
          # 1. Evitar que busque sysctl.h (No existe en Musl puro)
          find . -type f -name "juce_SystemStats_linux.cpp" -exec sed -i 's/#include <sys\/sysctl.h>/\/\/ #include <sys\/sysctl.h>/g' {} +
      
      # 4. Configurar el entorno limpio con CMake
      - name: Configurar CMake
        run: |
          export CFLAGS="-Wno-error -Wno-unused-function"
          export CXXFLAGS="-Wno-error -Wno-unused-function"
          export LDFLAGS="-lexecinfo"
          
          cmake -S BespokeSynth -B build \
            -DCMAKE_BUILD_TYPE=Release \
            -DBESPOKE_BUILD_AUTO_UPDATER=OFF
      
      # 5. Compilar el ejecutable nativo en paralelo (4 hilos)
      - name: Compilar ejecutable
        run: cmake --build build --config Release -j4

      # 6. Empaquetar los binarios listos en un archivo tar.gz
      - name: Preparar paquete de distribución
        run: |
          mkdir -p dist
          cp -r build/Source/BespokeSynth_artefacts/Release/* dist/ || cp -r build/BespokeSynth_artefacts/Release/* dist/ || cp -r build/Source/BespokeSynth_artefacts/* dist/
          tar -czvf bespokesynth-void-musl.tar.gz -C dist .

      # 7. Subir el artefacto a las Actions como respaldo temporal
      - name: Guardar copia en Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: BespokeSynth-Void-Musl
          path: bespokesynth-void-musl.tar.gz

      # 8. Generar el Release público permanente en la portada del repositorio
      - name: Crear Release Público Permanente
        uses: softprops/action-gh-release@v2
        if: success()
        with:
          tag_name: v${{ github.run_number }}-musl
          name: BespokeSynth Nativo para Musl (Build #${{ github.run_number }})
          body: |
            Binario nativo de BespokeSynth compilado de forma nativa en Void Linux Musl.
            ¡No requiere gcompat ni capas de traducción secundarias!
          draft: false
          prerelease: false
          files: bespokesynth-void-musl.tar.gz
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
